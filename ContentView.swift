import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Advanced Car Creator")
                .font(.largeTitle)
                .padding()
           
            Text("Create New Car Information")
                .font(.title3)
            Spacer()
        }
        
      }
    }

