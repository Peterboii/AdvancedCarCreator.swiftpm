import SwiftUI

struct ContentView: View {
    var body: some View {
      Text("Advanced Car Creator")
            
        Text("Create new car information")
        NavigationStack {
            NavigationLink {
                
            }
        }
    }
}
